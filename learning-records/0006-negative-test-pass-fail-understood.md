# Negative Test Pass/Fail Logic — Understood

Shivani correctly marked negative tests as "Passed" when the Juice Shop showed appropriate error messages (e.g., "Email address is not valid" for an email with no @ symbol). Confirmed she understands that a negative test Passes when the app correctly rejects bad input — not when it accepts it.

**Evidence:** She described the actual result ("shows email address is not valid under the email field") and had already marked it Passed — which is correct.

**Implications:** This is a solid conceptual foundation for bug reporting. She can now be introduced to the scenario where a negative test FAILS (app accepts bad input = bug). Lesson 03 (Bug Life Cycle) can use a real Juice Shop vulnerability as the example.
